import { CheckimagePipe } from "./checkimage.pipe";

describe("CheckimagePipe", () => {
  it("create an instance", () => {
    const pipe = new CheckimagePipe();
    expect(pipe).toBeTruthy();
  });
});
